# 数据更新

运行 `python scripts/update_data.py` 会读取 `data/fund_universe.json` 中的 33 只基金，并生成 `public/data/latest.json`。

网页不会在访问者的浏览器中抓取数据。每次成功运行后，页面读取的快照会包含生成时间、最新单位净值、净值日期、申购/赎回状态，以及场内 ETF 的最新市价。

## 数据口径

- 基金名单、跟踪指数和费率：本项目维护的基准主数据，来源为用户确认的研究报告资料。
- 最新净值和申赎状态：天天基金/东方财富历史净值接口。
- 场内 ETF 市价：东方财富公开行情接口。
- QDII 额度：国家外汇管理局 QDII 投资额度审批情况表，建议在每次额度更新时人工复核。

实时 IOPV 未被接入前，脚本不会用上一交易日净值代替 IOPV，也不会计算“溢价率”。这避免把不同时点数据误标为实时溢价。

## 发布流程

运行脚本后需重新构建并发布站点，新的 `latest.json` 才会公开生效。可将脚本配置为交易日收盘后的 Windows 任务计划程序任务；它不需要 AI 或 token。
